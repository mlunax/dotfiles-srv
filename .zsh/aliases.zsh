alias \
    wget='wget -c' \
    zshrc='source ~/.zshrc' \
    dps='docker ps -a --format "table {{.Names}}\t{{.Image}}\t{{.Status}}"' \
    c='docker-compose' \
    dots='git --git-dir=$HOME/.dotfiles --work-tree=$HOME' \
    lla='ls -lha' \
    cp='cp -iv' \
    rm='rm -iv' \
    mv='cp -iv' \
    mkd="mkdir -pv"
